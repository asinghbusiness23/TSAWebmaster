/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "www.pixalytics.com",
      "dummyimage.com",
      "chestercountyfoodbank.org",
    ],
  },
};

export default nextConfig;
